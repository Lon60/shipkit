apiVersion: apps/v1
kind: Deployment
metadata:
  name: k3s-control
  namespace: shipkit-system
spec:
  template:
    spec:
      containers:
      - name: server
        image: k3s-control:dev
        imagePullPolicy: IfNotPresent
