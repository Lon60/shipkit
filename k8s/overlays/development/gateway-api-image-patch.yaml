apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-api
  namespace: shipkit-system
spec:
  template:
    spec:
      containers:
      - name: gateway-api
        image: localhost/gateway-api:dev
        imagePullPolicy: IfNotPresent
