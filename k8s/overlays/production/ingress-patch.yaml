apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: shipkit-default
  namespace: shipkit-system
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: traefik-shipkit-https-redirect@kubernetescrd
spec:
  tls:
  - secretName: shipkit-tls
